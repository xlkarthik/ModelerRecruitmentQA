{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": ".*",
        "cmd": "git add -A && git commit -m \"Auto‐save commit\" && git push"
      }
    ]
  },
  // optional: automatically save files
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
